# Secrets — NEVER commit
.env
.env.local
*.env
!.env.example
secrets/
*.pem
*.key

# Node / build
node_modules/
dist/
*.log
.DS_Store

# asciinema cast source (gif is committed, cast is regenerable)
docs/demo.cast
